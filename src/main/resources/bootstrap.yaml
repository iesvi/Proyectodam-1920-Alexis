#Application Name
spring:
  application:
    name: gateway     # This is the name for the remote configuration file
  cloud:
    config:
      uri: http://54.161.173.48:8888  #where the config-service is running
      fail-fast: true             #the service will not run if it can't reach the config-service
